# All codes are ran in the Terminal
# After running the PerdictionModels.py file with the 'python' command, running the StreamlitePage.py file with 'streamlit run'command will lead you to the user interface. 
